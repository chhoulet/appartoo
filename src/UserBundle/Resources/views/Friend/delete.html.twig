{% extends '::base.html.twig' %}

	{% block title %}Remove{% endblock %}

	{% block body %}

		<div class="container">                     
            <div class="row">
                <div class="box">
                    <div class="col-lg-12">
                        <hr>
                        <h2 class="intro-text text-center">Remove a
                            <strong>friend </strong>
                        </h2>
                        <hr>
                    </div>
                    <div class="col-lg-12 text-center">
                    	{{form_start(formRem)}}
                    	{{form_row(formRem.users)}}
                    	<div class="col-md-12 text-center">
                    		{{form_end(formRem)}}
                    	</div>
                    </div>
                </div>
            </div>                    
        </div>
       
	{% endblock %}

	 